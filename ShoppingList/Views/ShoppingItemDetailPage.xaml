<?xml version="1.0" encoding="utf-8" ?>
<views:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:ShoppingList.Views"
             x:Class="ShoppingList.Views.ShoppingItemDetailPage"
             Title="{Binding Title}"
             BindingContext="{Binding ShoppingItemDetail, Source={StaticResource ViewModelLocator}}"
                >

    <StackLayout Spacing="20" Padding="15">
        <Label Text="Name:" FontSize="Medium" />
        <Label Text="{Binding Name}" FontSize="Small"/>
        <Label Text="State:" FontSize="Medium" />
        <Label Text="{Binding StateText}" FontSize="Small"/>
        <Label Text="Last bought:" FontSize="Medium" />
        <Label Text="{Binding LastBought}" FontSize="Small"/>
    </StackLayout>
    
</views:BasePage>